{
  "name": "comunic8",
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=web",
    "dev:server": "turbo run dev --filter=server",
    "build": "turbo run build",
    "db:push": "turbo run db:push --filter=server",
    "db:migrate": "turbo run db:migrate --filter=server",
    "db:studio": "turbo run db:studio --filter=server",
    "dev:desktop": "pnpm --filter web run tauri dev",
    "build:desktop": "pnpm --filter web run tauri build"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  }
}
